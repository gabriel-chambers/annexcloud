const ServerSideRendererError = _props => {
  return (
    <div className="p-3">
      <small>Delay in loading block: Use the refresh button in the top-right corner to reload the block</small>
    </div>
  )
}

export default ServerSideRendererError
